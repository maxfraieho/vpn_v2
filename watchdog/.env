# ═══════════════════════════════════════════════════════════════════
# WATCHDOG CONFIGURATION FILE
# Збережіть цей файл як: /opt/watchdog/.env
# Права доступу: chmod 600 /opt/watchdog/.env
# ═══════════════════════════════════════════════════════════════════

# ============== TAILSCALE НАЛАШТУВАННЯ ==============

# Tailscale IP адреса Termux пристрою з VPN/Proxy
PROXY_HOST="100.100.74.9"

# Порт проксі сервісу
# Типові порти:
#   8888 - tinyproxy, privoxy (HTTP)
#   3128 - Squid (HTTP)
#   1080 - SOCKS5
#   8080 - Alternative HTTP
PROXY_PORT="8888"

# Тип проксі: http, socks5, socks4
PROXY_TYPE="http"


# ============== CLOUDFLARE WORKER ==============

# URL вашого Cloudflare Worker
# Приклад: https://watchdog-notifier.your-name.workers.dev
WORKER_URL="https://watchdog-notifier.YOUR_SUBDOMAIN.workers.dev"


# ============== TELEGRAM ==============

# Токен Telegram бота (отримайте від @BotFather)
# Формат: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_TOKEN="YOUR_BOT_TOKEN_HERE"

# Chat ID для отримання сповіщень
# Отримайте від @userinfobot або через API
TELEGRAM_CHAT_ID="YOUR_CHAT_ID_HERE"


# ============== ПАРАМЕТРИ МОНІТОРИНГУ ==============

# Таймаут підключення (секунди)
CHECK_TIMEOUT=10

# Максимальна кількість спроб підключення
MAX_RETRIES=3

# Пауза між спробами (секунди)
RETRY_DELAY=5

# URL для тестування проксі функціональності
TEST_URL="http://example.com"


# ============== ЛОГУВАННЯ ==============

# Базова директорія Watchdog
BASE_DIR="/opt/watchdog"

# Директорія для логів
LOG_DIR="${BASE_DIR}/logs"

# Максимальний розмір лог файлу перед ротацією (байти)
# 10485760 = 10MB
MAX_LOG_SIZE=10485760

# Кількість днів для зберігання архівних логів
LOG_RETENTION_DAYS=30


# ============== ДОДАТКОВІ НАЛАШТУВАННЯ ==============

# Інтервал перевірки (хвилини) - встановлюється в systemd timer
# Цей параметр тільки для документації
CHECK_INTERVAL=20

# Частота сповіщень при тривалому збої
# Сповіщення надсилається кожні N послідовних помилок
NOTIFY_EVERY_N_ERRORS=5