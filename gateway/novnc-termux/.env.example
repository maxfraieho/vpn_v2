# SwissWorkspaceGateway environment configuration
# Copy to .env and edit values. Never commit .env to git.

# VNC security: "VncAuth" (default, requires password) or "None" (no password)
VNC_SECURITY=VncAuth

# Tailscale IP of this tablet (find with: tailscale ip -4)
TAILSCALE_IP=100.100.74.9

# websockify bind address.
# If unset: auto-detects Tailscale IP (recommended). Aborts if Tailscale unavailable.
# Set explicitly to override: Tailscale IP, 0.0.0.0, or 127.0.0.1
# WEBSOCKIFY_BIND=100.100.74.9

# Auto-start browser inside VNC session? (0=no, 1=yes)
# Default 0 for MVP stability. Use launch_browser.sh for manual browser launch.
START_BROWSER=0

# Browser to use when START_BROWSER=1 or via launch_browser.sh (firefox or chromium)
# Firefox is more stable in proot; Chromium triggers dbus/udev/netlink errors.
BROWSER=firefox

# Default URL when launching browser
START_URL=about:blank

# PRoot distro alias
PROOT_DISTRO=debian

# noVNC directory (cloned repo — adjust if repo path differs)
# NOVNC_DIR=$HOME/vpn_v2/gateway/novnc-termux/vendor/noVNC

# Workspace A
WS_A_DISPLAY=1
WS_A_VNC_PORT=5901
WS_A_NOVNC_PORT=6080
WS_A_GEOMETRY=1280x720
WS_A_BASE_DIR=$HOME/ws_a

# Workspace B
WS_B_DISPLAY=2
WS_B_VNC_PORT=5902
WS_B_NOVNC_PORT=6081
WS_B_GEOMETRY=1280x720
WS_B_BASE_DIR=$HOME/ws_b

# Optional HTTPS wrapper (Phase 2)
# HTTPS_PORT=6443
# HTTPS_CERT=/path/to/cert.pem
# HTTPS_KEY=/path/to/key.pem
# AUTH_USER=admin
# AUTH_PASS=changeme
